<div class="max-w-2xl mx-auto px-4 py-8">
  <h1 class="text-2xl font-semibold mb-4">Nova Turma</h1>
  <%= render 'form', turma: @turma %>
  <div class="mt-4">
    <%= link_to 'Voltar', turmas_path, class: 'text-sm text-gray-600' %>
  </div>
</div>