<%# Este wrapper principal e o cabeçalho vêm do seu formulário de turmas %>
<div class="w-full max-w-3xl mx-auto px-4 py-6">
  <div class="text-center mb-6">
    <h1 class="text-4xl font-extrabold text-[#C5A300] sm:text-5xl">
      <%# Altera o título dinamicamente se for um novo aluno ou edição %>
      <%= aluno.persisted? ? "Editar Aluno" : "Novo Aluno" %>
    </h1>
    <p class="mt-3 max-w-xl mx-auto text-lg text-gray-400">
      Preencha os detalhes abaixo para <%= aluno.persisted? ? "atualizar o" : "cadastrar um novo" %> aluno.
    </p>
  </div>

  <%# Aplicando o estilo principal do formulário de turmas %>
  <%= form_with(model: aluno, class: "bg-[#1C1C1C] rounded-2xl shadow-xl overflow-hidden border border-[#2E2E2E]") do |form| %>
    
    <%# Bloco de erros estilizado como o de turmas %>
    <% if aluno.errors.any? %>
      <div class="bg-red-900 bg-opacity-50 text-red-300 p-4 border-b border-red-800">
        <h3 class="font-bold mb-2"><%= pluralize(aluno.errors.count, "erro") %> impediu este aluno de ser salvo:</h3>
        <ul class="list-disc list-inside text-sm">
          <% aluno.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <%# Padding interno e layout em grid %>
    <div class="p-6">
      <div class="grid grid-cols-1 md:grid-cols-2 gap-x-5 gap-y-4">

        <%# --- DADOS DO ALUNO --- %>
        <h3 class="text-lg font-semibold text-gray-200 md:col-span-2 mt-4 first:mt-0 border-b border-[#2E2E2E] pb-1">
          Dados do Aluno
        </h3>

        <div class="md:col-span-2">
          <%= form.label :alu_nome, "Nome Completo", class: "flex items-center text-sm font-medium text-gray-300 mb-1.5" %>
          <% border_class = aluno.errors[:alu_nome].any? ? 'border-red-600 ring-1 ring-red-600' : 'border-[#2E2E2E] focus-within:border-[#C5A300] focus-within:ring-2 focus-within:ring-[#C5A300]' %>
          <div class="flex items-center bg-[#0A0A0A] rounded-lg border transition-colors duration-200 #{border_class}">
            <div class="pl-3.5 flex items-center pointer-events-none">
              <i class="fas fa-user w-5 text-center text-gray-500"></i>
            </div>
            <%= form.text_field :alu_nome, class: "block w-full bg-transparent text-[#E5E5E5] rounded-lg pl-2.5 pr-4 py-3 placeholder-gray-500 border-none focus:outline-none focus:ring-0", placeholder: "Nome completo do aluno" %>
          </div>
          <% if aluno.errors[:alu_nome].any? %>
            <p class="text-red-500 text-xs mt-1"><%= aluno.errors.full_messages_for(:alu_nome).first %></p>
          <% end %>
        </div>

        <div>
          <%= form.label :alu_data_nascimento, "Data de Nascimento", class: "flex items-center text-sm font-medium text-gray-300 mb-1.5" %>
          <% border_class = aluno.errors[:alu_data_nascimento].any? ? 'border-red-600 ring-1 ring-red-600' : 'border-[#2E2E2E] focus-within:border-[#C5A300] focus-within:ring-2 focus-within:ring-[#C5A300]' %>
          <div class="flex items-center bg-[#0A0A0A] rounded-lg border transition-colors duration-200 #{border_class}">
            <div class="pl-3.5 flex items-center pointer-events-none">
              <i class="fas fa-calendar-alt w-5 text-center text-gray-500"></i>
            </div>
            <%= form.date_field :alu_data_nascimento, class: "block w-full bg-transparent text-[#E5E5E5] rounded-lg pl-2.5 pr-4 py-3 placeholder-gray-500 border-none focus:outline-none focus:ring-0 [color-scheme:dark]" %>
          </div>
          <% if aluno.errors[:alu_data_nascimento].any? %>
            <p class="text-red-500 text-xs mt-1"><%= aluno.errors.full_messages_for(:alu_data_nascimento).first %></p>
          <% end %>
        </div>

        <div class="md:col-span-2">
          <%= form.label :alu_endereco, "Endereço", class: "flex items-center text-sm font-medium text-gray-300 mb-1.5" %>
          <% border_class = aluno.errors[:alu_endereco].any? ? 'border-red-600 ring-1 ring-red-600' : 'border-[#2E2E2E] focus-within:border-[#C5A300] focus-within:ring-2 focus-within:ring-[#C5A300]' %>
          <%# 'items-start' para alinhar o ícone com o topo do textarea %>
          <div class="flex items-start bg-[#0A0A0A] rounded-lg border transition-colors duration-200 #{border_class}">
            <div class="pl-3.5 pt-3.5 flex items-center pointer-events-none">
              <i class="fas fa-map-marker-alt w-5 text-center text-gray-500"></i>
            </div>
            <%= form.text_area :alu_endereco, rows: 3, class: "block w-full bg-transparent text-[#E5E5E5] rounded-lg pl-2.5 pr-4 py-3 placeholder-gray-500 border-none focus:outline-none focus:ring-0", placeholder: "Rua, Número, Bairro..." %>
          </div>
          <% if aluno.errors[:alu_endereco].any? %>
            <p class="text-red-500 text-xs mt-1"><%= aluno.errors.full_messages_for(:alu_endereco).first %></p>
          <% end %>
        </div>
        
        <div class="md:col-span-2">
          <%= form.label :foto, "Foto do Aluno", class: "flex items-center text-sm font-medium text-gray-300 mb-1.5" %>
          <% border_class = aluno.errors[:foto].any? ? 'border-red-600 ring-1 ring-red-600' : 'border-[#2E2E2E] focus-within:border-[#C5A300] focus-within:ring-2 focus-within:ring-[#C5A300]' %>
          <%# Estilo customizado para o file_field para parecer integrado %>
          <div class="bg-[#0A0A0A] rounded-lg border transition-colors duration-200 p-3 #{border_class}">
            <%= form.file_field :foto, class: "block w-full text-gray-400 file:mr-4 file:py-2 file:px-4 file:rounded-lg file:border file:border-[#C5A300] file:bg-black file:text-sm file:font-medium file:text-[#C5A300] hover:file:bg-[#1A1A1A] file:cursor-pointer focus:outline-none" %>
          </div>
          <% if aluno.errors[:foto].any? %>
            <p class="text-red-500 text-xs mt-1"><%= aluno.errors.full_messages_for(:foto).first %></p>
          <% end %>

          <%# Preview da foto atual, se existir %>
          <% if aluno.foto.attached? %>
            <div class="mt-3">
              <span class="text-sm font-medium text-gray-300 mb-1.5">Foto Atual:</span>
              <img src="<%= url_for(aluno.foto) %>" alt="<%= aluno.alu_nome %>" class="w-24 h-24 rounded-lg mt-2 object-cover border-2 border-[#2E2E2E]">
            </div>
          <% end %>
        </div>


        <%# --- DADOS DO RESPONSÁVEL --- %>
        <h3 class="text-lg font-semibold text-gray-200 md:col-span-2 mt-4 border-b border-[#2E2E2E] pb-1">
          Dados do Responsável
        </h3>
        
        <div class="md:col-span-2">
          <%= form.label :responsavel_nome, "Nome do Responsável", class: "flex items-center text-sm font-medium text-gray-300 mb-1.5" %>
          <% border_class = aluno.errors[:responsavel_nome].any? ? 'border-red-600 ring-1 ring-red-600' : 'border-[#2E2E2E] focus-within:border-[#C5A300] focus-within:ring-2 focus-within:ring-[#C5A300]' %>
          <div class="flex items-center bg-[#0A0A0A] rounded-lg border transition-colors duration-200 #{border_class}">
            <div class="pl-3.5 flex items-center pointer-events-none">
              <i class="fas fa-user-shield w-5 text-center text-gray-500"></i>
            </div>
            <%= form.text_field :responsavel_nome, class: "block w-full bg-transparent text-[#E5E5E5] rounded-lg pl-2.5 pr-4 py-3 placeholder-gray-500 border-none focus:outline-none focus:ring-0", placeholder: "Nome do pai, mãe ou responsável" %>
          </div>
          <% if aluno.errors[:responsavel_nome].any? %>
            <p class="text-red-500 text-xs mt-1"><%= aluno.errors.full_messages_for(:responsavel_nome).first %></p>
          <% end %>
        </div>

        <div>
          <%= form.label :responsavel_telefone, "Telefone do Responsável", class: "flex items-center text-sm font-medium text-gray-300 mb-1.5" %>
          <% border_class = aluno.errors[:responsavel_telefone].any? ? 'border-red-600 ring-1 ring-red-600' : 'border-[#2E2E2E] focus-within:border-[#C5A300] focus-within:ring-2 focus-within:ring-[#C5A300]' %>
          <div class="flex items-center bg-[#0A0A0A] rounded-lg border transition-colors duration-200 #{border_class}">
            <div class="pl-3.5 flex items-center pointer-events-none">
              <i class="fas fa-phone w-5 text-center text-gray-500"></i>
            </div>
            <%= form.text_field :responsavel_telefone, class: "block w-full bg-transparent text-[#E5E5E5] rounded-lg pl-2.5 pr-4 py-3 placeholder-gray-500 border-none focus:outline-none focus:ring-0", placeholder: "(XX) 9XXXX-XXXX" %>
          </div>
          <% if aluno.errors[:responsavel_telefone].any? %>
            <p class="text-red-500 text-xs mt-1"><%= aluno.errors.full_messages_for(:responsavel_telefone).first %></p>
          <% end %>
        </div>
        
        <div>
          <%= form.label :responsavel_email, "Email do Responsável", class: "flex items-center text-sm font-medium text-gray-300 mb-1.5" %>
          <% border_class = aluno.errors[:responsavel_email].any? ? 'border-red-600 ring-1 ring-red-600' : 'border-[#2E2E2E] focus-within:border-[#C5A300] focus-within:ring-2 focus-within:ring-[#C5A300]' %>
          <div class="flex items-center bg-[#0A0A0A] rounded-lg border transition-colors duration-200 #{border_class}">
            <div class="pl-3.5 flex items-center pointer-events-none">
              <i class="fas fa-envelope w-5 text-center text-gray-500"></i>
            </div>
            <%= form.email_field :responsavel_email, class: "block w-full bg-transparent text-[#E5E5E5] rounded-lg pl-2.5 pr-4 py-3 placeholder-gray-500 border-none focus:outline-none focus:ring-0", placeholder: "email@exemplo.com" %>
          </div>
          <% if aluno.errors[:responsavel_email].any? %>
            <p class="text-red-500 text-xs mt-1"><%= aluno.errors.full_messages_for(:responsavel_email).first %></p>
          <% end %>
        </div>


        <%# --- MATRÍCULA E SAÚDE --- %>
        <h3 class="text-lg font-semibold text-gray-200 md:col-span-2 mt-4 border-b border-[#2E2E2E] pb-1">
          Matrícula e Saúde
        </h3>

        <div>
          <%= form.label :alu_tur_codigo, "Turma", class: "flex items-center text-sm font-medium text-gray-300 mb-1.5" %>
          <% border_class = aluno.errors[:alu_tur_codigo].any? ? 'border-red-600 ring-1 ring-red-600' : 'border-[#2E2E2E] focus-within:border-[#C5A300] focus-within:ring-2 focus-within:ring-[#C5A300]' %>
          <div class="flex items-center relative bg-[#0A0A0A] rounded-lg border transition-colors duration-200 #{border_class}">
            <div class="pl-3.5 flex items-center pointer-events-none">
              <i class="fas fa-chalkboard-teacher w-5 text-center text-gray-500"></i>
            </div>
            <%= form.collection_select :alu_tur_codigo, @turmas, :tur_codigo, :tur_nome,
                  { include_blank: 'Selecione uma turma' },
                  { class: "block w-full appearance-none bg-transparent text-[#E5E5E5] rounded-lg pl-2.5 pr-10 py-3 border-none focus:outline-none focus:ring-0" } 
            %>
            <div class="absolute inset-y-0 right-0 pr-3.5 flex items-center pointer-events-none">
              <i class="fas fa-chevron-down text-gray-500 text-xs"></i>
            </div>
          </div>
          <% if aluno.errors[:alu_tur_codigo].any? %>
            <p class="text-red-500 text-xs mt-1"><%= aluno.errors.full_messages_for(:alu_tur_codigo).first %></p>
          <% end %>
        </div>

        <div>
          <%= form.label :alu_data_cadastro, "Data da Matrícula", class: "flex items-center text-sm font-medium text-gray-300 mb-1.5" %>
          <% border_class = aluno.errors[:alu_data_cadastro].any? ? 'border-red-600 ring-1 ring-red-600' : 'border-[#2E2E2E] focus-within:border-[#C5A300] focus-within:ring-2 focus-within:ring-[#C5A300]' %>
          <div class="flex items-center bg-[#0A0A0A] rounded-lg border transition-colors duration-200 #{border_class}">
            <div class="pl-3.5 flex items-center pointer-events-none">
              <i class="fas fa-calendar-check w-5 text-center text-gray-500"></i>
            </div>
            <%= form.date_field :alu_data_cadastro, value: (aluno.alu_data_cadastro || Date.today), class: "block w-full bg-transparent text-[#E5E5E5] rounded-lg pl-2.5 pr-4 py-3 placeholder-gray-500 border-none focus:outline-none focus:ring-0 [color-scheme:dark]" %>
          </div>
          <% if aluno.errors[:alu_data_cadastro].any? %>
            <p class="text-red-500 text-xs mt-1"><%= aluno.errors.full_messages_for(:alu_data_cadastro).first %></p>
          <% end %>
        </div>

        <div class="md:col-span-2">
          <%= form.label :alu_status, "Status", class: "flex items-center text-sm font-medium text-gray-300 mb-1.5" %>
          <% border_class = aluno.errors[:alu_status].any? ? 'border-red-600 ring-1 ring-red-600' : 'border-[#2E2E2E] focus-within:border-[#C5A300] focus-within:ring-2 focus-within:ring-[#C5A300]' %>
          <div class="flex items-center relative bg-[#0A0A0A] rounded-lg border transition-colors duration-200 #{border_class}">
            <div class="pl-3.5 flex items-center pointer-events-none">
              <i class="fas fa-toggle-on w-5 text-center text-gray-500"></i>
            </div>
            <%= form.select :alu_status, 
                  options_for_select(['Ativo', 'Inativo', 'Pendente'], (aluno.alu_status || 'Ativo')), 
                  { }, 
                  { class: "block w-full appearance-none bg-transparent text-[#E5E5E5] rounded-lg pl-2.5 pr-10 py-3 border-none focus:outline-none focus:ring-0" } 
            %>
            <div class="absolute inset-y-0 right-0 pr-3.5 flex items-center pointer-events-none">
              <i class="fas fa-chevron-down text-gray-500 text-xs"></i>
            </div>
          </div>
          <% if aluno.errors[:alu_status].any? %>
            <p class="text-red-500 text-xs mt-1"><%= aluno.errors.full_messages_for(:alu_status).first %></p>
          <% end %>
        </div>

        <div class="md:col-span-2">
          <%= form.label :condicoes_medicas, "Condições Médicas (Descrição)", class: "flex items-center text-sm font-medium text-gray-300 mb-1.5" %>
          <% border_class = aluno.errors[:condicoes_medicas].any? ? 'border-red-600 ring-1 ring-red-600' : 'border-[#2E2E2E] focus-within:border-[#C5A300] focus-within:ring-2 focus-within:ring-[#C5A300]' %>
          <div class="flex items-start bg-[#0A0A0A] rounded-lg border transition-colors duration-200 #{border_class}">
            <div class="pl-3.5 pt-3.5 flex items-center pointer-events-none">
              <i class="fas fa-briefcase-medical w-5 text-center text-gray-500"></i>
            </div>
            <%= form.text_area :condicoes_medicas, rows: 3, class: "block w-full bg-transparent text-[#E5E5E5] rounded-lg pl-2.5 pr-4 py-3 placeholder-gray-500 border-none focus:outline-none focus:ring-0", placeholder: "Ex: Asma, alergia a lactose..." %>
          </div>
          <% if aluno.errors[:condicoes_medicas].any? %>
            <p class="text-red-500 text-xs mt-1"><%= aluno.errors.full_messages_for(:condicoes_medicas).first %></p>
          <% end %>
        </div>

      </div>
    </div>

    <%# Botões de ação estilizados como os de turma %>
    <div class="bg-[#111111] px-6 py-4 border-t border-[#2E2E2E] flex flex-col sm:flex-row sm:items-center sm:justify-end gap-3">
      <%= link_to alunos_path, 
            class: 'w-full sm:w-auto inline-flex justify-center items-center gap-2 px-6 py-2.5 bg-red-600 text-white font-bold rounded-lg shadow-md border border-red-600 hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-offset-black focus:ring-red-500 transition-all transform hover:scale-105 cursor-pointer' do %>
        <span>Cancelar</span>
      <% end %>
      
      <%= form.submit (aluno.persisted? ? 'Atualizar Aluno' : 'Salvar Aluno'), 
            class: 'w-full sm:w-auto inline-flex justify-center items-center gap-2 px-6 py-2.5 bg-black text-[#C5A300] font-bold rounded-lg shadow-md border border-[#C5A300] hover:bg-[#1A1A1A] hover:text-[#D4AF37] focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-offset-black focus:ring-[#C5A300] transition-all transform hover:scale-105 cursor-pointer' 
      %>
    </div>

  <% end %>
</div>