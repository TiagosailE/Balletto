<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">

  <div class="flex flex-col sm:flex-row sm:justify-between sm:items-center gap-4 mb-8">
    <div>
      <h1 class="text-3xl font-bold text-gray-900 dark:text-white flex items-center gap-3">
        <div class="w-12 h-12 bg-gradient-to-br from-[#C5A300] to-[#B88B00] rounded-xl flex items-center justify-center">
          <i class="fas fa-user-graduate text-white text-xl"></i>
        </div>
        Gerenciar Alunos
      </h1>
      <p class="text-gray-500 dark:text-gray-400 mt-2 ml-15">
        <%= @alunos.count %> <%= @alunos.count == 1 ? 'aluno encontrado' : 'alunos encontrados' %>
      </p>
    </div>

    <%= link_to new_aluno_path, class: "group inline-flex items-center justify-center gap-2 px-6 py-3 bg-gradient-to-r from-[#C5A300] to-[#D4AF37] text-white font-semibold rounded-xl shadow-lg hover:shadow-xl hover:scale-105 transition-all duration-200" do %>
      <i class="fas fa-plus group-hover:rotate-90 transition-transform duration-200"></i>
      <span class="hidden sm:inline">Adicionar Novo Aluno</span>
      <span class="sm:hidden">Novo Aluno</span>
    <% end %>
  </div>

  <div class="grid grid-cols-1 lg:grid-cols-4 gap-6 mb-8">

    <div class="lg:col-span-1">
      <div class="bg-white dark:bg-[#1C1C1C] rounded-2xl shadow-lg border border-gray-200 dark:border-[#2E2E2E] p-6 **sticky top-6 max-h-[calc(100vh-48px)] overflow-y-auto**">
        <h3 class="text-lg font-bold text-gray-900 dark:text-white mb-4 flex items-center gap-2">
          <i class="fas fa-filter text-[#C5A300]"></i>
          Filtros
        </h3>

        <div class="p-1 bg-gray-100 dark:bg-[#0F0F0F] rounded-lg mb-4 space-y-1">
          <a href="<%= alunos_path %>" class="block w-full text-center px-4 py-2.5 font-semibold bg-[#C5A300] text-black rounded-lg">Matriculados</a>
          <a href="<%= possiveis_alunos_path %>" class="block w-full text-center px-4 py-2.5 font-medium text-gray-600 dark:text-gray-400 hover:bg-white dark:hover:bg-[#1C1C1C] rounded-lg transition-colors">
        Possíveis Alunos
      </a>
        </div>

        <%= form_with(url: alunos_path, method: :get, class: "space-y-3") do |form| %>

          <div class="relative">
            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
              <i class="fas fa-search text-gray-400 dark:text-gray-500"></i>
            </div>
            <%= form.text_field :query,
                value: params[:query],
                placeholder: "Buscar aluno...",
                class: "w-full pl-10 pr-4 py-2.5 bg-gray-50 dark:bg-[#0F0F0F] text-gray-900 dark:text-white border border-gray-300 dark:border-[#2E2E2E] rounded-lg text-sm" %>
          </div>

          <div>
            <label class="block text-xs font-semibold text-gray-600 dark:text-gray-400 mb-2 uppercase">Status</label>
            <%= form.select :status,
                [["Todos", ""], ["Ativo", "Ativo"], ["Inativo", "Inativo"], ["Pendente", "Pendente"]],
                { selected: params[:status] },
                class: "w-full px-3 py-2.5 bg-gray-50 dark:bg-[#0F0F0F] text-gray-900 dark:text-white border border-gray-300 dark:border-[#2E2E2E] rounded-lg text-sm" %>
          </div>

          <div>
            <label class="block text-xs font-semibold text-gray-600 dark:text-gray-400 mb-2 uppercase">Turma</label>
            <%= form.select :turma,
                @turmas.map { |t| [t.tur_nome, t.tur_codigo] }.prepend(["Todas", ""]),
                { selected: params[:turma] },
                class: "w-full px-3 py-2.5 bg-gray-50 dark:bg-[#0F0F0F] text-gray-900 dark:text-white border border-gray-300 dark:border-[#2E2E2E] rounded-lg text-sm" %>
          </div>

          <button type="submit" class="w-full px-4 py-2.5 bg-white dark:bg-black text-[#C5A300] font-semibold rounded-lg border border-[#C5A300]">
            Aplicar Filtros
          </button>
        <% end %>

        <div class="mt-6 pt-6 border-t border-gray-200 dark:border-[#2E2E2E]">
          <div class="space-y-3">
            <div class="flex items-center justify-between">
              <span class="text-sm text-gray-600 dark:text-gray-400">Total</span>
              <span class="text-lg font-bold text-gray-900 dark:text-white"><%= @alunos.count %></span>
            </div>
            <div class="flex items-center justify-between">
              <span class="text-sm text-gray-600 dark:text-gray-400">Ativos</span>
              <span class="text-sm font-semibold text-green-600 dark:text-green-400"><%= @alunos.where(alu_status: 'Ativo').count %></span>
            </div>
            <div class="flex items-center justify-between">
              <span class="text-sm text-gray-600 dark:text-gray-400">Inativos</span>
              <span class="text-sm font-semibold text-gray-600 dark:text-gray-400"><%= @alunos.where(alu_status: 'Inativo').count %></span>
            </div>
          </div>
        </div>
      </div>
    </div>

    <div class="lg:col-span-3">

      <% if @alunos.empty? %>

        <% if @total_alunos == 0 %>

          <div class="bg-white dark:bg-[#1C1C1C] rounded-2xl shadow-lg border border-gray-200 dark:border-[#2E2E2E] p-12 text-center">
            <div class="w-20 h-20 mx-auto mb-6 bg-gradient-to-br from-gray-100 to-gray-200 dark:from-[#0F0F0F] dark:to-[#1A1A1A] rounded-full flex items-center justify-center">
              <i class="fas fa-user-graduate text-3xl text-gray-400 dark:text-gray-600"></i>
            </div>
            <h3 class="text-xl font-bold text-gray-900 dark:text-white mb-2">
              Nenhum aluno encontrado
            </h3>
            <p class="text-gray-500 dark:text-gray-400 mb-8 max-w-md mx-auto">
              Comece adicionando seu primeiro aluno ao sistema.
            </p>
            <%= link_to new_aluno_path, class: "inline-flex items-center gap-2 px-5 py-3 bg-gradient-to-r from-[#C5A300] to-[#D4AF37] text-white font-semibold rounded-xl shadow-lg" do %>
              <i class="fas fa-plus"></i>
              <span>Adicionar Primeiro Aluno</span>
            <% end %>
          </div>

        <% else %>

          <div class="bg-white dark:bg-[#1C1C1C] rounded-2xl shadow-lg border border-gray-200 dark:border-[#2E2E2E] p-12 text-center">
            <div class="w-20 h-20 mx-auto mb-6 bg-gradient-to-br from-gray-100 to-gray-200 dark:from-[#0F0F0F] dark:to-[#1A1A1A] rounded-full flex items-center justify-center">
              <i class="fas fa-filter text-3xl text-gray-400 dark:text-gray-600"></i>
            </div>
            <h3 class="text-xl font-bold text-gray-900 dark:text-white mb-2">
              Nenhum aluno encontrado para os filtros selecionados
            </h3>
            <p class="text-gray-500 dark:text-gray-400 mb-8 max-w-md mx-auto">
              Ajuste os filtros e tente novamente.
            </p>
          </div>

        <% end %>

      <% else %>

        <div class="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-4">

          <% @alunos.each do |aluno| %>
  <div class="group bg-white dark:bg-[#1C1C1C] rounded-xl shadow-lg border border-gray-200 dark:border-[#2E2E2E] hover:shadow-xl hover:scale-[1.03] transition-all duration-200 overflow-hidden relative">

    <%# 1. O LINK TERMINA AQUI, COBRINDO APENAS O CONTEÚDO VISUAL %>
    <%= link_to aluno_path(aluno), class: "block h-full" do %>
      <div class="relative h-28 bg-gradient-to-br from-[#C5A300] to-[#D4AF37] overflow-visible">
        <div class="absolute inset-0 bg-black opacity-0 group-hover:opacity-10 transition-opacity"></div>

        <% status_config = {
            "Ativo" => { color: "bg-green-600", icon: "fa-check-circle" },
            "Inativo" => { color: "bg-gray-600", icon: "fa-times-circle" },
            "Pendente" => { color: "bg-yellow-500", icon: "fa-clock" }
          }
          sc = status_config[aluno.alu_status] || status_config["Pendente"]
        %>

        <div class="absolute top-2 right-2">
          <span class="inline-flex items-center gap-1 px-2 py-0.5 text-white text-xs font-bold rounded-full <%= sc[:color] %> shadow">
            <i class="fas <%= sc[:icon] %>"></i>
            <%= aluno.alu_status %>
          </span>
        </div>

        <div class="absolute left-1/2 bottom-0 translate-y-1/2 transform -translate-x-1/2">
          <div class="w-20 h-20 rounded-full border-4 border-white dark:border-[#1C1C1C] shadow-xl bg-white flex items-center justify-center overflow-hidden">
            <% if aluno.foto.attached? %>
              <%= image_tag aluno.foto.variant(resize_to_fill: [400, 400]), class: "w-full h-full object-cover" %>
            <% else %>
              <span class="text-2xl font-bold text-[#C5A300]">
                <%= aluno.alu_nome.split.map(&:first).join.upcase[0..1] %>
              </span>
            <% end %>
          </div>
        </div>
      </div>

      <div class="pt-14 pb-5 px-4 text-center">
        <h3 class="text-base font-bold text-gray-900 dark:text-white mb-1">
          <%= aluno.alu_nome %>
        </h3>

        <div class="flex items-center justify-center gap-2 text-xs text-gray-500 dark:text-gray-400 mb-3">
          <i class="fas fa-chalkboard-teacher text-xs"></i>
          <span><%= aluno.turma&.tur_nome || 'Sem turma' %></span>
        </div>

        <div class="grid grid-cols-2 gap-3 pt-3">
          <div class="text-center">
            <p class="text-[10px] text-gray-500 dark:text-gray-400 mb-0.5">Idade</p>
            <p class="text-sm font-bold text-gray-900 dark:text-white">
              <% if aluno.alu_data_nascimento %>
                <%= ((Date.current - aluno.alu_data_nascimento.to_date) / 365.25).to_i %> anos
              <% else %>
                -
              <% end %>
            </p>
          </div>

          <div class="text-center">
            <p class="text-[10px] text-gray-500 dark:text-gray-400 mb-0.5">Matrícula</p>
            <p class="text-sm font-bold text-gray-900 dark:text-white">
              <% if aluno.alu_data_cadastro %>
                <%= aluno.alu_data_cadastro.year %>
              <% else %>
                -
              <% end %>
            </p>
          </div>
        </div>

        <% if aluno.responsavel_nome %>
          <div class="mt-3 pt-3 border-t border-gray-100 dark:border-[#2E2E2E]">
            <div class="flex items-center gap-2 text-xs text-gray-600 dark:text-gray-400 justify-center">
              <i class="fas fa-user-shield text-[#C5A300]"></i>
              <span class="truncate"><%= aluno.responsavel_nome %></span>
            </div>
          </div>
        <% end %>
      </div>

      <div class="pb-4 flex items-center justify-center gap-2 text-xs text-gray-500 dark:text-gray-400 group-hover:text-[#C5A300]">
        <i class="fas fa-eye"></i>
        <span>Visualizar</span>
      </div>
    <% end %> 
    <%# AQUI O LINK FECHOU %>

    <%# 2. O BOTÃO AGORA ESTÁ FORA DO LINK, MAS DENTRO DA DIV RELATIVE %>
    <button 
      type="button"
      class="absolute bottom-2 right-2 p-2 text-red-600 dark:text-red-400 hover:text-red-700 dark:hover:text-red-500 transition-colors duration-200 z-50 cursor-pointer"
      data-confirm-modal="true"
      data-confirm-title="Excluir Aluno: <%= aluno.alu_nome %>"
      data-confirm-body="Você tem certeza que deseja excluir permanentemente o aluno <%= aluno.alu_nome %>? Esta ação é irreversível."
      data-confirm-path="<%= aluno_path(aluno) %>"
      data-confirm-text="Excluir"
      data-confirm-class="bg-red-600 hover:bg-red-500"
      title="Excluir Aluno"
    >
      <i class="fas fa-trash-alt text-lg pointer-events-none"></i>
    </button>

  </div>
<% end %>

        </div>

      <% end %>

    </div>
  </div>

</div>

<%= render 'shared/confirm_modal' %>